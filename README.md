# Plate Generator (React + Vite, JavaScript)

Configure wall plates (in centimeters) and preview how a shared motif image is segmented across them in real-time.

## Quickstart
```bash
npm i
npm run dev